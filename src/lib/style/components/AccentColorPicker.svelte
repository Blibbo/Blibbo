<script lang="ts">



let hex = $state(null);
let rgb = $state(null);
let hsv = $state(null);
let color = $state(null);

// $effect(()=>{
//   // console.log(hex);
//   // if(hex !=== );

//   // i need to save the state of the default color
//   if(!hex)return;
//   setAccent(hex);
// })

</script>

